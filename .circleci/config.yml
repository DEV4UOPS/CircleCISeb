version: 2.1
jobs:
  build:
    docker:
      - image: mattrayner/lamp:latest-1804
        
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Install Docker client
          command: |
            sudo apt-get update
            sudo apt-get install docker-ce-cli            
